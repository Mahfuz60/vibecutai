package com.vibecut.ai.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary — Electric Violet ─────────────────────────────────────────────────
val VibePrimary           = Color(0xFF9D5CFF)  // Electric violet
val VibeOnPrimary         = Color(0xFF1A0050)
val VibePrimaryContainer  = Color(0xFF4A1F8A)
val VibeOnPrimaryContainer = Color(0xFFEADDFF)
val VibePrimaryLight      = Color(0xFF6B3CC7)

// ── Secondary — Magenta accent ────────────────────────────────────────────────
val VibeSecondary         = Color(0xFFE040FB)  // Magenta/neon pink
val VibeOnSecondary       = Color(0xFF30003F)
val VibeSecondaryContainer = Color(0xFF6A007F)
val VibeOnSecondaryContainer = Color(0xFFFDD6FF)

// ── Tertiary — Cyan highlight ─────────────────────────────────────────────────
val VibeTertiary          = Color(0xFF00E5FF)  // Neon cyan
val VibeOnTertiary        = Color(0xFF003B45)

// ── Backgrounds — deep black ──────────────────────────────────────────────────
val VibeBackground        = Color(0xFF0A090F)  // Near black with purple tint
val VibeOnBackground      = Color(0xFFEAE1F8)

// ── Surfaces ──────────────────────────────────────────────────────────────────
val VibeSurface           = Color(0xFF131018)  // Card/sheet background
val VibeOnSurface         = Color(0xFFEAE1F8)
val VibeSurfaceVariant    = Color(0xFF1E1B28)  // Slightly lighter surface
val VibeOnSurfaceVariant  = Color(0xFFCBBFE8)

// ── Functional ────────────────────────────────────────────────────────────────
val VibeError             = Color(0xFFFF5449)
val VibeOnError           = Color(0xFF690005)
val VibeOutline           = Color(0xFF4D4460)

// ── Extra palette for UI ──────────────────────────────────────────────────────
val VibeGold              = Color(0xFFFFD700)
val VibeNeonGreen         = Color(0xFF00FF87)
val VibeTimeline          = Color(0xFF1C172B)  // Timeline track background
val VibeTimelineThumb     = Color(0xFFB688FF)  // Playhead / thumb color